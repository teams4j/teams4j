org.gradle.jvmargs=-Xmx1g

# The version under test. Kept in step with the library's own gradle.properties; until 0.1.0 is on
# Central it is a SNAPSHOT, so run `./gradlew publishToMavenLocal` in the parent directory first.
# testcontainers-java bumps the same kind of property from a release-triggered workflow.
teams4jVersion=0.1.0-SNAPSHOT

# Boot version for :spring-boot. Override to check the other Boot line:
#   ./gradlew :spring-boot:run -PbootVersion=4.1.1
bootVersion=3.5.16
